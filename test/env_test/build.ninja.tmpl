rule collect_env
  command = rm -f "$$SHELLVARS_FILE" "$out" && touch "$$SHELLVARS_FILE" && {{VARIABLES}} && sed -i.bak -e 's|'"$$INSTALLDIR"'|{{INSTALLDIR}}|g' -e 's|'"$$EXT_BUILD_ROOT"'|{{EXT_BUILD_ROOT}}|g' "$$SHELLVARS_FILE" && touch "$out"

build out: collect_env

default out
