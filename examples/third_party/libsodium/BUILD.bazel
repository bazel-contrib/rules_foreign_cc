load("@bazel_skylib//rules:build_test.bzl", "build_test")

exports_files(
    [
        "BUILD.libsodium.bazel",
    ],
    visibility = ["//visibility:public"],
)

build_test(
    name = "libsodium_build_test",
    targets = [
        "@libsodium//:libsodium",
    ],
    visibility = ["//:__pkg__"],
)
